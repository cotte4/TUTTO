// This component has been deprecated as data is now managed via Google Sheets.
// It is kept as a placeholder to prevent import errors during migration.
import React from 'react';

export const AdminPage: React.FC<any> = () => {
  return null;
};